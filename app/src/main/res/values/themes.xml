<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MyApplication" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>

        <item name="colorPrimary">@color/neon_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="appBackgroundColor">@color/white</item>
        <item name="appCardBackgroundColor">#F5F5F5</item>
        <item name="appHeaderTextColor">@color/black</item>
        <item name="appContentTextColor">@color/black</item>
        <item name="appSubTextColor">#80000000</item>
        <item name="appIconTint">@color/black</item>
        <item name="appDividerColor">#E0E0E0</item>
        <item name="bottomNavIconColor">@color/bottom_nav_color</item>

        <!-- Preference Styling -->
        <item name="preferenceTheme">@style/PreferenceTheme</item>
    </style>

    <style name="PreferenceTheme" parent="@style/PreferenceThemeOverlay">
        <item name="preferenceCategoryStyle">@style/PreferenceCategoryStyle</item>
    </style>

    <style name="PreferenceCategoryStyle" parent="Preference.Category">
        <item name="android:layout">@layout/preference_category_layout</item>
    </style>

    <style name="Theme.MyApplication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.MyApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Utility Styles -->
    <style name="roundedCornerStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <style name="circleStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>